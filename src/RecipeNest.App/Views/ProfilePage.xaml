<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipeNest.Views.ProfilePage"
             Title="ProfilePage">
    <ScrollView>
        <VerticalStackLayout>
            <Label Text="Profile Page"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Button Text="TEST" Command="{Binding SynchronizeCommand}"></Button>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>